Kubernetes Day-3 ( 07-03-2026 04:11 PM IST)
------------------------------------------------
3 machine --> Ubuntu 22.04 (Jammy)---> cri-dockerd--> debian package

Ubuntu 24.04 (Nobel) ---> we don't have cri-dockerd

cri-dockerd --> CRI - Container Run time 

Kubernetes ---> Docker

Kubernetes ---> Interface (CRI) cri-dockerd--> Docker

CNI ---> Waeve network -- Basic Network plugin --> less feature
Cloud based k8s services:
----------------------------

AWS - EKS
Azure - AKS
GCP - GKE

--------------------------------------
We will do the fresh kubernetes setup again using kubeadm method
--------------------------

Azure --> 2 VM's ---> 2VCPU ,4 GB Ram
Ubuntu - 24.04 version

containerd--> container

CNI - Flannel , Calico

Master ---> login as root user ---> kubeadm init 

kubeadm init --cri-socket unix:///var/run/cri-dockerd.sock

kubectl join

docker ---> docker enpoint  , 
containerd ---> containerd endpoint

kubeadm init -->enter

Calico ---> extra arugument --> --pod-network-cidr

-----------------------------------------------------------
Kubernetes Installation , containerd & Calico installation steps:

---------------------------------------------------------------------------

Step-1: Installing containerd 

sudo apt update
sudo apt install -y containerd

containerd --version

sudo mkdir -p /etc/containerd
containerd config default | sudo tee /etc/containerd/config.toml

sudo vi /etc/containerd/config.toml
SystemdCgroup = false
SystemdCgroup = true

sudo systemctl restart containerd
sudo systemctl enable containerd
sudo systemctl status containerd

sudo modprobe overlay
sudo modprobe br_netfilter

cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF


cat <<EOF | sudo tee /etc/sysctl.d/kubernetes.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF


sudo sysctl --system
----------------------------------------------------------------------------------

Step-2: Installing kubeadm kubectl,kubelet

Refer to official site to get all commands

----------------------------------------------------------------------------------

Step-3: Kubernetes initlization, you need to login as root user

 kubeadm init \
--apiserver-advertise-address=10.0.0.4 \
--pod-network-cidr=10.100.0.0/16



---------------------------------------------------------------------------------------

Step-4: Installing calico cni plugin

kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.27.3/manifests/calico.yaml

kubectl get pods -n kube-system
kubectl get pods -n kube-system -l k8s-app=calico-node

kubectl run nginx --image=nginx
kubectl get pods -o wide
-------------------------------------------------------------

To start using your cluster, you need to run the following as a regular user:

  mkdir -p $HOME/.kube
  sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
  sudo chown $(id -u):$(id -g) $HOME/.kube/config

Alternatively, if you are the root user, you can run:

  export KUBECONFIG=/etc/kubernetes/admin.conf

You should now deploy a pod network to the cluster.
Run "kubectl apply -f [podnetwork].yaml" with one of the options listed at:
  https://kubernetes.io/docs/concepts/cluster-administration/addons/

Then you can join any number of worker nodes by running the following on each as root:

kubeadm join 10.0.0.4:6443 --token 5k670d.d2u2qg6idzjc6ish \
        --discovery-token-ca-cert-hash sha256:2398494f2d7055122a7abffd2ac0b3aca902f3a6c60509b86fe7de97ac34fcdf --cri-socket unix:///run/containerd/containerd.sock
		
-----------------------------------------------------------------------------

Kubernetes Workloads -> Pod
--------------------------------------

Docker create a container, Hypervisor create the virtual machines.

Kubernetes will create Pods

Containers are not directly allowed inside the kubernetes cluster

Pod - smallest deployable unit in cluster. A container will be there inside a Pod

Pods has the container in it.

Every Pod gets a unique ip within a k8s clustder

Pod can have one or more container in it .
Containers within a pod communicate over localhost (127.0.0.1)

Two container cannot be inside the pod which require same ports.

In a Pod if we have more than one container
-----------------------------------
1. Primary container ---> maincar container
2. other container ---> sidecar containers


Ideally a Pod should run an application/microservice in it .


